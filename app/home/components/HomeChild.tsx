export default async function HomeChild() {
  // const data = await fetch("http://localhost:3000/api/log/dc/test").then(
  //   (res) => res.json()
  // );

  return <section>HomeChild Page</section>;
}
